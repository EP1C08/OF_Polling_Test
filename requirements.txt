# OnlyFans API
ultima-scraper-api>=2.2.45

# HTTP & Async
aiohttp>=3.9.0
asyncio

# User Agent Generation
user-agent>=0.1.10

# Data Processing
python-dateutil>=2.8.2
python-dotenv>=1.0.0
pandas>=2.0.0

# Redis
redis>=5.0.0

# PostgreSQL (for database worker)
asyncpg>=0.29.0

# SQLAlchemy (for WebSocket system and database queries)
sqlalchemy[asyncio]>=2.0.0

# Encryption (for database credentials)
cryptography>=41.0.0

# File locking (for pending fans JSON)
filelock>=3.12.0

# YAML for docker-compose generation
pyyaml>=6.0

# GoLogin API (proxy management)
gologin>=2025.4

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
aioresponses>=0.7.6
